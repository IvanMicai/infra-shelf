# Screenshots

Drop web-UI screenshots here so the main README can reference them. Suggested
captures (start the UI with `make app`, then visit http://127.0.0.1:8080):

- `apps-list.png` — the dashboard listing provisioned apps.
- `app-detail.png` — an app's page with the "Reveal credentials" action.
- `backups.png` — backup history and the S3 upload action.
- `schedules.png` — creating a schedule with retention (Keep days / Keep files).

Once added, wire them into the README's Screenshots section, e.g.:

```markdown
| Apps | Credentials |
| :---: | :---: |
| ![Apps](docs/screenshots/apps-list.png) | ![Credentials](docs/screenshots/app-detail.png) |
```
