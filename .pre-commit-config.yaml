# Optional secret-scanning hook. Install with:
#   pipx install pre-commit   (or: brew install pre-commit)
#   pre-commit install
# Then secrets are scanned on every commit. See SECURITY.md.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
